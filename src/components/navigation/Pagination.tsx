import React from 'react'

const Pagination = ({ currentPage, setCurrentPage }: { currentPage: number, setCurrentPage: () => void }) => {
  return (
    <div>

    </div>
  )
}

export default Pagination
